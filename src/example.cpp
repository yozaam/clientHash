int squarer(int num) {
  return num * num;
}
//inserting this function code in https://mbebenita.github.io/WasmExplorer/ 

//it will return a wat with a line like this
// (export "_Z7squareri" (func $_Z7squareri))
// this is now the function name "_Z7squareri" that we use through javascript
// download the .wasm and put it alongside an index.html to access the function
